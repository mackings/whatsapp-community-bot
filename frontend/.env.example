VITE_API_BASE_URL=http://localhost:4000
# VITE_WS_URL is optional — by default it's derived from VITE_API_BASE_URL
# (same host, ws(s):// instead of http(s)://, path /ws). Only set this if
# the WebSocket needs to be reached at a different address than the API.
